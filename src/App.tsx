import React from 'react';

import { SafeAreaView } from 'react-native';
import { NewsFeed } from '@feed/scenes';

const App = () => {
  return (
    <SafeAreaView>
      <NewsFeed />
    </SafeAreaView>
  );
};

export default App;
